# Results

`results/reference_outputs/` contains final machine-readable outputs used to verify Paper 1 Axis A results. They are reference outputs, not raw inputs.

`results/reproduced/` is ignored by Git and can be used for regenerated outputs.
